version: "3.8"

services:

  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nginx
    networks:
      - domic
    ports:
      - "443:443"
      - "80:80"

networks:
  domic:
    external: true